version: '2'
services:
          dico_topo_mysql_prod:
                  container_name: dico_topo_mysql_prod
                  restart: always
                  image: mysql:5.6
                  ports:
                          - "3309:3306"
                  environment:
                          MYSQL_ROOT_PASSWORD: 'root'
                          MYSQL_USER: 'dicotopo'
                          MYSQL_PASS: 'dicotopo'
                  volumes:
                   - dico-topo-datavolume-prod:/var/lib/mysql
volumes:
        dico-topo-datavolume-prod: {}
